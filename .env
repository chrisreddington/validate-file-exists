# Environment variables for local testing of the validate-file-exists action
# These variables simulate the inputs that would be provided when running the action

# Comma-separated list of files to validate
# Example: Check if package.json, README.md, and tsconfig.json exist
INPUT_REQUIRED-FILES=package.json,README.md,tsconfig.json

# Optional: Set debug mode for more verbose output
# ACTIONS_STEP_DEBUG=true

# Optional: Set runner debug mode
# ACTIONS_RUNNER_DEBUG=true
